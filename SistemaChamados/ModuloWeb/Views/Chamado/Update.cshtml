@model Model.Entity.Chamado
@{
    ViewBag.Title = "Dados do Chamado";
}

@using (Html.BeginForm())
{
    <div class="form-horizontal" summary="Tabela com dados do chamado a serem alterados" style="padding:10px;">
        <fieldset>
            <div class="form-group">
                @Html.LabelFor(model => model.Protocolo, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-2">
                    @Html.EditorFor(model => model.Protocolo, new { htmlAttributes = new { @class = "form-control", @readonly = "readonly" } })
                </div>
            </div>
            <div class="form-group">
                @Html.LabelFor(model => model.DataAbertura, htmlAttributes: new { @class = "control-label col-md-2", @readonly = "readonly" })
                <div class="col-md-4">
                    @Html.EditorFor(model => model.DataAbertura, new { htmlAttributes = new { @class = "form-control", @readonly = "readonly" } })
                    <small class="alert-danger">*Campo obrigatório - (máx 50 caracteres)</small>
                </div>
            </div>
            <div class="form-group">
                @Html.LabelFor(model => model.DescChamado, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-4">
                    @Html.EditorFor(model => model.DescChamado, new { htmlAttributes = new { @class = "form-control" } })
                    <small class="alert-danger">*Campo obrigatório</small>
                </div>
            </div>
            <div class="form-actions col-md-offset-2">
                <div class="form-group col-md-3">
                    <input class="btn btn-primary" type="submit" name="btnsalvar" value="Salvar" />
                </div>
                <div class="form-group col-md-3">
                    <a class="btn btn-info" href="~/Chamado/ConsultarChamadoAberto">Voltar</a>
                </div>
            </div>
        </fieldset>
    </div>
}

<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Mensagem</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="text-primary text-success text-center">
                    @ViewBag.MensagemExito
                </div>
                <div class="text-primary text-danger text-center">
                    @ViewBag.MensagemErro
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Fechar</button>
            </div>
        </div>
    </div>
</div>

@if (ViewBag.MensagemExito != null || ViewBag.MensagemErro != null)
{
    @section Scripts{
        <script>
            $(function () {
                $('#exampleModal').modal('show');
            });
        </script>
    }

}

